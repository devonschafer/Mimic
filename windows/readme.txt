For the windows batch file, change install_pip.txt to
install_pip.bat by Save As. Then double-click to run.
This will install guizero, mouse, keyboard python modules.

If you already have these installed, then no need to run this.

